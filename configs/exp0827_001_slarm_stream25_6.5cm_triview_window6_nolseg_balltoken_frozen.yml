# 6.5cm ball token · A 组：冻结 backbone，只训 ball token
#
# 目的：在 6.5cm 上干净验证「ball token 直接回归球心」能否胜过逐像素反投影。
#   backbone 取 exp0825_002 训满 40k 的产物，冻住不动，唯一有梯度的是新加的
#   ball_query / ball_block / ball_head。唯一变量就是 ball token 本身。
#
# ─────────────────────────────────────────────────────────────
# 这组要打败的东西是什么（2026-08-26 verify 结果，已修正早先的判断）：
#
#   像素法链路是三步：语义选球 → depth 反投影 → 多视角 median。
#   verify --limit 40 在 ckpt_039999 上跑出来：
#       pred mask  pos15 median 0.0364 m
#       gt   mask  pos15 median 0.0375 m
#   两者几乎相同 —— 换成完美的球 mask 也不会更好，**语义选球不是瓶颈**。
#   （ball_iou farthest 只有 0.194，但那说的是"选中的像素少"，
#     少数选中像素的中心位置仍然是对的，所以 median 不受影响。）
#
#   误差实际来自第二步：沿射线方向（深度）占误差能量的 95.5%，
#   横向只占 4.5%。也就是说球的方向找得准，距离估得不准。
#
#   所以 ball token 的卖点不是"绕开语义头"，而是：
#     1. 绕开 depth 反投影 —— 直接从 token 回归 3D 坐标，不经过深度图；
#     2. 三视角 token 在 attention 里联合推断，天然带三角化信息，
#        而像素法是各视角独立反投影后再取 median（median 消不掉共同的深度偏置）。
#   24cm 上 ball token 赢 38% 时也是 pred≈gt 的局面，与这个解释一致。
#
#   如果这组输了，说明 aggregator 的 token 里根本没有足够的深度信息，
#   问题在表征层而不在读出方式 —— 那时该做的是加多视角几何约束，而不是调 ball head。
# ─────────────────────────────────────────────────────────────
#
# 判据 —— 注意基线不是一个数，是一条带（2026-08-27 verify_sweep）：
#
#   扫 exp0825_002 的 5 个 ckpt，pos15 median 分别是
#       029999 0.0312   033999 0.0353   035999 0.1106   037999 0.0623   039999 0.0364
#   摆动 3.5×，且没有任何 ckpt 全面占优（median 最好 029999，p95 最好 033999 = 0.0644，
#   v15 最好 037999 = 0.0724）。评测集逐 ckpt 完全相同，所以这全是权重本身的抖动 ——
#   stage1 用 constant LR 跑满 40k、无衰减无 EMA，每个存档都只是盆地里的一次采样。
#
#   但这条带**不影响本组的主判据**，因为 verify 的对比是配对的：
#   同一次 verify 里同时报 pred（像素法）和 balltoken 两路，同一批 40 场景、同一组权重。
#   所以看的是"同一个 ckpt 上，两条读出路径谁更准"，而不是"对比另一个 ckpt 记下的数"。
#   backbone 的抖动被配对设计吃掉了。
#
#   带的作用在于判断**结论能不能外推**：
#     · balltoken 显著低于同一次 verify 的 pred  → 在这个 backbone 上成立；
#     · 但要说"ball token 这条路成立"，还得在退火后的稳定 backbone 上复现一次，
#       否则可能只是这个采样点的性质。
#
#   跑 tools/verify_physics_extrapolation.py --limit 40 --ball-mask-source pred，
#   看 balltoken 那一路对 pred 那一路。训练中直接看 stream25_ball_pos_l2_m（单位米）。
#   6.5cm 球径 0.065 m —— 误差超过 0.065 就等于没指到球。
model: slarm
dataset: [ball_catch_6.5cm_triview]
data_root: data/SLARM_data_6.5

input_size: [320, 240]
patch_size: 8
embed_dim: 768
depth: 12
patch_embed: conv
num_max_cameras: 3
num_context_timesteps: 6
num_target_timesteps: 7
context_stride: 3
timespan: 0.8
mode: window_6

use_affine_token: true
use_sky_token: true
use_ms3_motion: true
use_last_token: true
shortcut_rgb: true
add_patch_plucker_embed: true
sigmoid_rgb: true
num_motion_tokens: 0

strict_data_loading: true
enable_task_semantic_head: true
terminal_context_extrapolation: true
stream25_render_target_chunk_size: 7
stream25_reconstruction_loss: true

# ---- ball token（本实验主角）----
use_ball_token: true
ball_token_freeze_backbone: true      # ★ A 组：冻结。只有 ball_query/ball_block/ball_head 有梯度
stream25_ball_pos_weight: 1.0
stream25_ball_vel_weight: 1.0
# 备注：24cm 上实测 ball_vel15_error≈0.176 m/s，乘 dt≈0.3 后对 frame24 的贡献
# 与 pos15 误差量级相当。若 pos 已收敛而 frame24 仍偏大，可把 vel 权重提到 2.0 再试。

# ---- 损失权重：与 exp0825_002 完全一致 ----
# 冻结下 dense head 无梯度，这些项照算但不影响参数；保持一致是为了日志可直接横比。
stream25_rgb_weight: 1.00
stream25_lpips_weight: 0.05
stream25_ball_rgb_weight: 0.50
stream25_depth_relative_weight: 1.00
stream25_ball_depth_metric_weight: 0.5
stream25_semantic_weight: 1.00
stream25_ms3_ball_weight: 1.00
stream25_ms3_static_weight: 0.25
stream25_opacity_weight: 0.10

stream25_ms3_velocity_scale: 5.0
stream25_ms3_acceleration_scale: 9.81
stream25_ms3_jerk_scale: 1.0
terminal_dynamic_velocity_threshold: 1.0
stream25_semantic_class_weight_cap: 10.0

# ---- 无 LSeg ----
online_feat: false
enable_feat_loss: false
feat_loss_coeff: 0.0
stream25_lseg_feature_weight: 0.0
enable_depth_loss: true
enable_sky_opacity_loss: true
enable_perceptual_loss: true
perceptual_weight: 0.05
perceptual_loss_start_iter: 0
load_depth: true

# ---- 优化（只训新初始化的 ball token：正常 LR + 短 warmup + 短训）----
lr: 1.0e-4                   # ball token 是随机初始化的新模块，需要正常量级的 LR
stream25_trunk_lr: 2e-5      # 冻结下 trunk 无梯度，此值无实际作用（保留仅为与 B 组对齐）
lr_sched: constant
min_lr: 1.0e-6
warmup_iters: 500            # 只训 ball token，收敛快，短 warmup 足够
batch_size: 1
seed: 20260827               # ★ 与 B 组同 seed：让两组的 ball_query 初始值相同，可比

# ★ 从 6.5cm stage1（exp0825_002 训满 40k）继续。ckpt 里没有 ball_* 权重，
#   misc.load_model 是 strict=False，这三个模块会随机初始化 —— 这正是本实验想要的。
#   为什么用 029999 而不是末点 039999：
#   029999 是 sweep 里像素法 pos15 最好的一个点（0.0312）。冻结训练下 backbone 不动，
#   这意味着 ball token 面对的是**像素法处于最佳状态**的对手 —— 这是一次偏保守的比较，
#   在这里赢下来才可信。反过来选一个像素法状态差的 ckpt（比如 035999 的 0.1106），
#   赢了也只是在跟一个坏基线比。
#
#   注意这跟"按 argmin 挑 ckpt"不是一回事：这里挑的不是"对 ball token 最有利的起点"，
#   而是"对 ball token 最不利的起点"，方向正好相反，不存在向评测集过拟合的问题。
load_from: work_dirs/slarm/exp0825_002_slarm_stream25_6.5cm_triview_window6_nolseg_loadpre/checkpoints/ckpt_029999.pth

train_annotation: scene_list/ball_catch_6.5cm_triview_train.txt
eval_annotation: scene_list/ball_catch_6.5cm_triview_validation.txt
num_iterations: 10000        # 冻结只训 ball token，10k 足够收敛
ckpt_every_n_iters: 1000
vis_every_n_iters: 2000
num_vis_samples: 1
eval_every_n_iters: 50000
keep_n_ckpts: 10
project: slarm
exp_name: exp0827_001_slarm_stream25_6.5cm_triview_window6_nolseg_balltoken_frozen
